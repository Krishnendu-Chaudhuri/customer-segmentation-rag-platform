# Data Directory

Place the 8 dunnhumby Complete Journey CSV files here:

- `transaction_data.csv`
- `product.csv`
- `hh_demographic.csv`
- `campaign_table.csv`
- `campaign_desc.csv`
- `coupon.csv`
- `coupon_redempt.csv`
- `causal_data.csv`

This directory is **gitignored** (`data/*.csv`). CSV files are not committed to the repository.
